
server:
  error:
    whitelabel:
      enabled: true
    include-message: ALWAYS
    include-stacktrace: ALWAYS
    include-exception: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-components: always
      show-details: always
